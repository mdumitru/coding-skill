---
name: execute-workflow
description: Use when the user's message contains the exact keyword `execute:`, including the trailing colon, followed by a TODO file. Work through the TODO items one by one, marking each complete and committing after each.
---

# `execute:` Workflow

Follow `execute-workflow.md` in this skill directory.
