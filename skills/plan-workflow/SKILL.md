---
name: plan-workflow
description: Use when the user's message contains the exact keyword `plan:`, including the trailing colon. Investigate the request and produce a structured, checkbox-based TODO markdown file that another AI agent can execute item by item.
---

# `plan:` Workflow

Follow `plan-workflow.md` in this skill directory.
